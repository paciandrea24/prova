# Luna Park Funland — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Costruire il quartiere Luna Park "Funland" (recinto ~50×38 m a nord della piazza) collegato a piazza (arco ingresso sud), Jazz (varco NE) e Galleria (passaggio di servizio est), assorbendo il flank nord demolito; tutto statico, coaster come skyline.

**Architecture:** Asset GLB da Blender 5.1 headless via `funland_lib.py` (riusa galleria_lib/jazz_lib come piazza_lib); zona caricata da `loadZone` esistente (COL_* AABB locali → OBB per istanza); il flank nord `fN` esce da `collegamenti.glb`, sostituito da un corridoio di servizio a L (x88→97, z=−36 → portale N Galleria); rete di sicurezza e soffitto per-zona estesi; +4 spawn server.

**Tech Stack:** Blender 5.1 headless (`C:\Program Files\Blender Foundation\Blender 5.1\blender.exe --background --python <script>`), Three.js r128 (fps.js), Node/Express (spawn server).

## Global Constraints

- **NON committare mai**: i commit li fa SOLO l'utente. I passi "commit" classici sono sostituiti da gate di verifica.
- Coordinate di gioco: x est, z sud (**nord = −z**), y su. Blender: `bx=x_gioco, by=−z_gioco, bz=y_gioco` (usare sempre `box_game`/helper, mai coordinate crude).
- Le mesh `COL_*` dentro un GLB devono essere **AABB locali** (niente rotazioni intra-GLB): la rotazione arriva SOLO dal `rotY` dell'istanza (loadZone → `addSolidOBB`).
- Ogni asset visivo passa un **gate di render preview** (utente) prima di considerarsi fatto; mai solo primitive nude (lathe/skin/bevel).
- Niente emoji nelle insegne (testo/glyph only). Stile "boxy ma dettagliato", palette circo (crema/rosso/salvia/ottone) coerente col mondo.
- **Tutto statico**: nessuna animazione (decisione spec); niente sistemi nuovi in fps.js oltre a quelli indicati al Task 11.
- Winding from_pydata: la mappa `(lx,−lz)` gioco→Blender SPECCHIA il winding → costruire i quad in ordine invertito (bug storico pavimento nero).
- Anti z-fighting: mai due superfici orizzontali sovrapposte con Δy < 2 cm.
- Geometria Funland (costanti in funland_lib): recinto `X0,X1 = 38,88`, `Z0,Z1 = −52,−18`; arco ingresso (55.5,−17.5) bocca w5 (x 53..58); giostra (56,−34); ruota (78,−46); pista autoscontro centro (72,−32) 14×10; ristoro (~48.5,−24); tiro a segno (44,−40); cancello est (88,−36); varco NE Jazz (32.5,−31.8); portale N Galleria (97,−31.6). Coaster fondale a z≈−55 (fuori recinto).
- ⚠️ I **retri dei palazzi Jazz** (facciate a r45.5, corpi verso l'esterno fino a r≈52-55) invadono il lato SO del parco: le posizioni SW (ristoro, recinzione sud-ovest) vanno validate sul **render combinato con i GLB jazz reali** (stile `debug_junction.py`), non solo sulle sagome.
- Server: `node server.js` DALLA cartella `backend/` — lo avvia SOLO l'utente. Test headless sul server che l'utente tiene acceso.
- Cartelle: `frontend/assets/models/funland/` (nuova), `collegamenti-wip/` (rigenerata in place), `piazza/` (si edita solo il layout JSON).

---

### Task 1: funland_lib + smoke test pipeline

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/funland_lib.py`
- Create: `docs/superpowers/plans/blender-scripts/funland/smoke.py`

**Interfaces:**
- Produces: modulo `funland_lib` con costanti geometria (`X0,X1,Z0,Z1,ARCO,GIOSTRA,RUOTA,PISTA,RISTORO,TIRO,GATE_EST,VARCO_JZ`), `box_game(...)`, `materiali()`, re-export `clear_scene, flat_material, export_glb, render_previews, add_box, add_box_c, add_cyl` (galleria_lib) e `lathe_profile, skin_chain` (jazz_lib). `MODELS_DIR = frontend/assets/models/funland`, `PREVIEW_DIR = blender-scripts/funland/preview`.

- [ ] **Step 1: Scrivere funland_lib.py**

```python
# funland_lib.py — libreria Luna Park Funland (riusa galleria_lib + jazz_lib)
import sys, os, math
REPO = "C:/Users/pacia/Desktop/Claude Workspace/prova"
SCRIPTS = REPO + "/docs/superpowers/plans/blender-scripts"
sys.path.insert(0, SCRIPTS)                  # jazz_lib.py
sys.path.insert(0, SCRIPTS + "/galleria")    # galleria_lib.py
import bpy
import galleria_lib as gl
import jazz_lib as jl

MODELS_DIR = REPO + "/frontend/assets/models/funland"
PREVIEW_DIR = SCRIPTS + "/funland/preview"
gl.MODELS_DIR = MODELS_DIR
gl.PREVIEW_DIR = PREVIEW_DIR
jl.MODELS_DIR = MODELS_DIR
jl.PREVIEW_DIR = PREVIEW_DIR

# ── Geometria parco (coordinate MONDO, nord = −z) ──
X0, X1 = 38.0, 88.0        # recinto E-O
Z0, Z1 = -52.0, -18.0      # recinto N-S (coaster fondale oltre Z0)
ARCO    = (55.5, -17.5)    # arco ingresso, bocca w5 (x 53..58); gola z −16..−18
GIOSTRA = (56.0, -34.0)
RUOTA   = (78.0, -46.0)
PISTA   = (72.0, -32.0)    # autoscontro 14×10 → x 65..79, z −37..−27
RISTORO = (48.5, -24.0)
TIRO    = (44.0, -40.0)
GATE_EST = (88.0, -36.0)   # cancello → corridoio servizio → portale N Galleria (97,−31.6)
VARCO_JZ = (32.5, -31.8)   # varco NE Jazz (porta ovest, raccordo_ovest)

clear_scene = gl.clear_scene
flat_material = gl.flat_material
export_glb = gl.export_glb
render_previews = gl.render_previews
add_box = gl.add_box
add_box_c = gl.add_box_c
add_cyl = gl.add_cyl
lathe_profile = jl.lathe_profile
skin_chain = jl.skin_chain


def box_game(name, w, d, h, gx, gz, gy, mat, bevel=0.02):
    # w lungo x_gioco, d lungo z_gioco, h altezza; base a gy.
    return gl.add_box(name, w, d, h, gx, -gz, gy, mat, bevel)


MAT = None
def materiali():
    # Palette circo. Nomi = nomi materiale GLB: il toon-swap fa cache PER NOME,
    # riusare i nomi jazz/piazza dove il colore coincide (crema, ottone, legno...).
    global MAT
    MAT = {
        'crema':   flat_material('crema',    (0.90, 0.86, 0.74, 1)),
        'rosso':   flat_material('marquee_rosso', (0.59, 0.14, 0.13, 1)),
        'salvia':  flat_material('verde_salvia', (0.45, 0.56, 0.42, 1)),
        'verde':   flat_material('verde_scuro', (0.15, 0.31, 0.29, 1)),
        'ottone':  flat_material('ottone',   (0.74, 0.53, 0.22, 1)),
        'legno':   flat_material('legno',    (0.34, 0.20, 0.11, 1)),
        'nero':    flat_material('nero',     (0.12, 0.12, 0.13, 1)),
        'bianco':  flat_material('bianco_giostra', (0.92, 0.90, 0.85, 1)),
        'prato':   flat_material('prato',    (0.33, 0.52, 0.24, 1)),
        'terra':   flat_material('terra_fiera', (0.50, 0.40, 0.28, 1)),
        'metallo': flat_material('metallo_pista', (0.42, 0.44, 0.48, 1)),
        'giallo':  flat_material('giallo_cantiere', (0.86, 0.68, 0.20, 1)),
        'blu':     flat_material('blu_fiera', (0.24, 0.36, 0.55, 1)),
        'sanp_a':  flat_material('sanp_a',   (0.36, 0.34, 0.31, 1)),
        'sanp_b':  flat_material('sanp_b',   (0.45, 0.44, 0.41, 1)),
        'sanp_c':  flat_material('sanp_c',   (0.55, 0.53, 0.49, 1)),
        'cordolo': flat_material('cordolo',  (0.70, 0.68, 0.63, 1)),
    }
    return MAT


def testo(nome, txt, size, mat, loc, rot=(math.radians(90), 0, 0)):
    # Insegna: text mesh estruso, centrato. loc in coordinate BLENDER del modello.
    bpy.ops.object.text_add()
    t = bpy.context.object
    t.data.body = txt
    t.data.size = size
    t.data.extrude = 0.03
    t.data.align_x = 'CENTER'
    bpy.ops.object.convert(target='MESH')
    t.name = nome
    t.location = loc
    t.rotation_euler = rot
    t.data.materials.append(mat)
    return t
```

- [ ] **Step 2: Scrivere smoke.py**

```python
import sys
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
fl.box_game("smoke_box", 2, 2, 1, 0, 0, 0, M['rosso'])
fl.testo("smoke_txt", "FUNLAND", 0.5, M['giallo'], (0, -1.2, 1.2))
fl.render_previews("smoke", ortho_scale=8)
print("SMOKE OK — GIOSTRA =", fl.GIOSTRA, "GATE_EST =", fl.GATE_EST)
```

- [ ] **Step 3: Eseguire** — Run (PowerShell): `& "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --background --python "docs\superpowers\plans\blender-scripts\funland\smoke.py"`
Expected: `SMOKE OK — GIOSTRA = (56.0, -34.0) GATE_EST = (88.0, -36.0)` + PNG in `blender-scripts/funland/preview/`.
- [ ] **Step 4: Read dei PNG** — box e scritta visibili (pipeline ok).

---

### Task 2: Collegamenti — demolizione fN + corridoio servizio est

**Files:**
- Modify: `docs/superpowers/plans/blender-scripts/collegamenti/collegamenti-layout-wip.py:220-243`
- Output: `frontend/assets/models/collegamenti-wip/collegamenti.glb` + `collegamenti-layout.json` rigenerati

**Interfaces:**
- Consumes: helper esistenti del file (`wall_seg`, `box_game`, `M`, `flank_u`).
- Produces: mondo SENZA flank nord; corridoio `srv_*` a L largo 3.5 h4 (semi-coperto nero) dal cancello est Funland (88,−36) al portale N Galleria (97,−31.6, porta verde esistente). Flank SUD e pezzi piazza INTATTI. ⚠️ Da questo task fino al Task 11 il varco NE Jazz dà sul void: transitorio atteso.

- [ ] **Step 1: Eliminare il flank nord** — rimuovere SOLO la riga:

```python
flank_u("fN", 32.5, -31.8, -38.0, 97.0, -31.6, WF, HF, M['sabbia'], M['nero'])
```

sostituendola col commento:

```python
# fN DEMOLITO (spec 2026-07-10): il flanking nord lo fa l'anello esterno di
# Funland; il tratto verso il portale N Galleria è il corridoio srv_* qui sotto.
```

(la def `flank_u` resta: la usa fS; la def `jazz_gateway` resta: chiamata da flank_u)

- [ ] **Step 2: Aggiungere il corridoio di servizio** subito dopo la riga di `flank_u("fS", ...)`:

```python
# ── PASSAGGIO DI SERVIZIO EST (Funland → portale N Galleria) ──
# L: braccio E-O a z=-36 dal cancello est del parco (x=88) a x≈97, poi gamba a
# sud fino al portale N (97,-31.6): stessa geometria d'arrivo del vecchio fN,
# la sigillatura lato galleria resta la porta verde `portale_varco`.
W_SRV, H_SRV = 3.5, 4.0
hs = W_SRV / 2
wall_seg("srv_o_eo", 88.0, -36.0 - hs, 97.0 + hs, -36.0 - hs, H_SRV, M['sabbia'])   # esterno nord
wall_seg("srv_o_ns", 97.0 + hs, -36.0 - hs, 97.0 + hs, -31.6, H_SRV, M['sabbia'])   # esterno est
wall_seg("srv_i_eo", 88.0, -36.0 + hs, 97.0 - hs, -36.0 + hs, H_SRV, M['sabbia'])   # interno sud (si ferma al gomito)
wall_seg("srv_i_ns", 97.0 - hs, -36.0 + hs, 97.0 - hs, -31.6, H_SRV, M['sabbia'])   # interno ovest
box_game("srv_pav_eo", 9.0 + hs, W_SRV, 0.12, (88.0 + 97.0 + hs) / 2, -36.0, -0.12, M['pav'])
box_game("srv_pav_ns", W_SRV, 4.4 + hs, 0.12, 97.0, (-36.0 - hs - 31.6) / 2, -0.12, M['pav'])
box_game("srv_roof_eo", 9.0 + hs, W_SRV + 0.4, 0.2, (88.0 + 97.0 + hs) / 2, -36.0, H_SRV, M['nero'])
box_game("srv_roof_ns", W_SRV + 0.4, 4.4 + hs, 0.2, 97.0, (-36.0 - hs - 31.6) / 2, H_SRV, M['nero'])
```

- [ ] **Step 3: Aggiornare le `vie` del JSON** (documentativo) — sostituire la riga `flank_nord` con:

```python
        {"nome": "servizio_est", "asse": "x", "da": 88, "a": 97, "centro": -36, "larghezza": WF},
```

- [ ] **Step 4: Rigenerare** — Run: `& "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --background --python "docs\superpowers\plans\blender-scripts\collegamenti\collegamenti-layout-wip.py"`
Expected: GLB+JSON riscritti, top render `collegamenti_top_debug.png`.
- [ ] **Step 5: Esaminare il top render** — niente più U a z=−38; la L di servizio aggancia il portale N; flank sud intatto a z=+38.

---

### Task 3: Pavimentazione (`funland_base.glb`)

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/funland-pavimentazione.py`
- Output: `frontend/assets/models/funland/funland_base.glb`

**Interfaces:**
- Produces: GLB `funland_base` con origine al CENTRO recinto (istanza a x=63, z=−35, y=−0.02). Griglia di celle 2×2: percorsi (viale, anello, piazzole) in sanpietrini top bz 0.10, verde/terra top bz 0.06 (Δ4 cm, niente z-fight). Include la GOLA d'ingresso (x 51..60, z −16.05..−18, testa a filo dell'ovale piazza SENZA sovrapporlo). Celle con centro a `hypot<45.8` dal centro Jazz SALTATE (clamp come piazza). Nessuna COL.

- [ ] **Step 1: Scrivere la ricetta**

```python
import sys, math, random
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import bpy
import funland_lib as fl

fl.clear_scene()
M = fl.materiali()
random.seed(24)
CX, CZ = (fl.X0 + fl.X1) / 2, (fl.Z0 + fl.Z1) / 2   # (63, -35) = origine modello

# Percorsi (rettangoli in coordinate MONDO): viale, anello esterno, piazzole
PATHS = [
    (51.0, 60.5, -36.0, -16.0),   # viale ingresso→giostra
    (fl.X0, fl.X1, -52.0, -47.5), # anello: lato nord
    (83.5, fl.X1, -47.5, -18.0),  # anello: lato est
    (60.0, 83.5, -22.5, -18.0),   # anello: lato sud-est (verso cancello/pista)
    (33.0, 42.0, -50.0, -30.0),   # anello: lato ovest (dal varco Jazz alla NW)
    (49.0, 66.0, -40.0, -28.0),   # piazzola giostra
    (40.0, 51.0, -44.0, -36.0),   # piazzola tiro a segno
    (64.0, 84.0, -38.0, -26.0),   # piazzola autoscontro
    (72.0, 84.0, -50.0, -40.0),   # piazzola ruota
    (44.0, 53.0, -28.0, -21.0),   # piazzola ristoro
]
def is_path(x, z):
    return any(a <= x <= b and c <= z <= d for (a, b, c, d) in PATHS)

CELL = 2.0
per_mat = {k: ([], []) for k in ('sanp_a', 'sanp_b', 'sanp_c', 'prato', 'terra_fiera')}
def cella(mname, x0g, z0g, x1g, z1g, h):
    verts, faces = per_mat[mname]
    b = len(verts)
    # ordine INVERTITO (winding specchiato dalla mappa (x,−z))
    corners = [(x1g, z0g), (x0g, z0g), (x0g, z1g), (x1g, z1g)]
    for (gx, gz) in corners:
        verts.append((gx - CX, -(gz - CZ), 0.0))
    for (gx, gz) in corners:
        verts.append((gx - CX, -(gz - CZ), h))
    faces += [(b+4, b+5, b+6, b+7),
              (b+0, b+1, b+5, b+4), (b+1, b+2, b+6, b+5),
              (b+2, b+3, b+7, b+6), (b+3, b+0, b+4, b+7)]

x = fl.X0 - 6.0                       # sborda a ovest fino ai retri jazz (poi clamp cerchio)
while x < fl.X1:
    z = fl.Z0
    while z < fl.Z1:
        cx, cz = x + CELL / 2, z + CELL / 2
        if math.hypot(cx, cz) < 45.8:       # sotto la pavimentazione/palazzi Jazz
            z += CELL; continue
        if is_path(cx, cz):
            cella(random.choice(('sanp_a', 'sanp_b', 'sanp_c')), x, z, x + CELL, z + CELL, 0.10)
        else:
            cella('prato' if random.random() < 0.7 else 'terra_fiera', x, z, x + CELL, z + CELL, 0.06)
        z += CELL
    x += CELL

# GOLA d'ingresso: tappeto sanpietrini dall'ovale piazza (z=-16) al recinto (z=-18)
zz = -18.0
while zz < -16.05:
    xx = 51.0
    while xx < 60.0:
        cella(random.choice(('sanp_a', 'sanp_b', 'sanp_c')), xx, zz, min(xx + CELL, 60.0), min(zz + CELL, -16.05), 0.10)
        xx += CELL
    zz += CELL

for mname, (verts, faces) in per_mat.items():
    mesh = bpy.data.meshes.new("pav_" + mname)
    mesh.from_pydata(verts, [], faces)
    mesh.update()
    obj = bpy.data.objects.new("pav_" + mname, mesh)
    bpy.context.collection.objects.link(obj)
    obj.data.materials.append(M[mname if mname != 'terra_fiera' else 'terra'])

fl.export_glb("funland_base.glb")
fl.render_previews("funland_base", ortho_scale=62, quarter_pos=(40, -50, 35))
```

(NB: la chiave del dict per_mat 'terra_fiera' corrisponde al nome materiale; M la indicizza con 'terra' → la ricetta lo gestisce nell'append. Verificare la firma di `render_previews` in galleria_lib e adeguare i parametri.)

- [ ] **Step 2: Render + esame** — leggere i PNG: viale e anello leggibili come nastri chiari sul verde, gola presente, bordo ovest "morso" dal clamp circolare.
- [ ] **Step 3: Gate utente** sulla preview.

---

### Task 4: Recinzione, cancello, recinto basso, raccordo ovest + layout perimetro

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/recinzione.py` (asset multipli via ARGV)
- Create: `docs/superpowers/plans/blender-scripts/funland/funland-layout.py`
- Output: `frontend/assets/models/funland/recinzione.glb`, `cancello.glb`, `recinto_basso.glb`, `raccordo_ovest.glb`, `funland-layout.json` (versione perimetro)

**Interfaces:**
- Produces: `recinzione.glb` = modulo 4×0.25 h2.5, origine centro-base, lungo X locale, con pilastrini alle estremità e festone di bandierine; `COL_recinzione` 4×0.3×2.5. `cancello.glb` = varco w3.5 incorniciato (2 montanti + architrave a y3..4); COL montanti+architrave. `recinto_basso.glb` = 3×0.2 h1.05 (crouch cover), COL piena. `raccordo_ovest.glb` = 2 ali piene 8×0.6 h4 ai lati di un varco w3.5 centrato nell'origine lungo X locale + architrave y3..4 (versione instanziabile del jazz_gateway: le COL locali sono AABB, la rotazione la dà il rotY dell'istanza). `funland-layout.py` = generatore layout completo (questo task: base+perimetro; i task 5-10 aggiungono istanze).

- [ ] **Step 1: Ricetta recinzione.py** (blocchi `if ASSET == ...`, override da CLI con `-- <nome>`):

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
ASSET = 'recinzione'
if '--' in sys.argv:
    ASSET = sys.argv[sys.argv.index('--') + 1]
fl.clear_scene()
M = fl.materiali()

if ASSET == 'recinzione':
    for s in (-1, 1):
        fl.box_game(f"pilastro_{s}", 0.3, 0.3, 2.7, s * 2.0, 0, 0, M['crema'])
        fl.box_game(f"cima_{s}", 0.4, 0.4, 0.15, s * 2.0, 0, 2.7, M['rosso'], bevel=0.05)
    for i in range(9):
        fl.box_game(f"doga_{i}", 0.18, 0.10, 2.2, -1.6 + i * 0.4, 0, 0.15, M['salvia'])
    fl.box_game("corrente_alto", 4.0, 0.12, 0.15, 0, 0, 2.35, M['crema'])
    fl.box_game("corrente_basso", 4.0, 0.12, 0.15, 0, 0, 0.15, M['crema'])
    for i in range(6):     # bandierine triangolari appese al corrente alto
        fl.add_box_c(f"band_{i}", 0.22, 0.04, 0.30, -1.45 + i * 0.58, 0, 2.15,
                     M['rosso'] if i % 2 == 0 else M['crema'], rot=(0, math.radians(40), 0))
    fl.box_game("COL_recinzione", 4.0, 0.3, 2.5, 0, 0, 0, M['crema'])
    fl.export_glb("recinzione.glb")
    fl.render_previews("recinzione", ortho_scale=6)

if ASSET == 'cancello':
    for s in (-1, 1):
        fl.box_game(f"montante_{s}", 0.45, 0.45, 3.2, s * 1.98, 0, 0, M['rosso'])
        fl.box_game(f"COL_montante_{s}", 0.45, 0.45, 3.2, s * 1.98, 0, 0, M['rosso'])
        fl.box_game(f"palla_{s}", 0.35, 0.35, 0.3, s * 1.98, 0, 3.2, M['ottone'], bevel=0.1)
    fl.box_game("architrave", 4.4, 0.35, 0.7, 0, 0, 3.0, M['crema'], bevel=0.05)
    fl.box_game("COL_architrave", 4.4, 0.5, 1.0, 0, 0, 3.0, M['crema'])
    fl.export_glb("cancello.glb")
    fl.render_previews("cancello", ortho_scale=6)

if ASSET == 'recinto_basso':
    for s in (-1, 1):
        fl.box_game(f"paletto_{s}", 0.2, 0.2, 1.05, s * 1.45, 0, 0, M['crema'])
    fl.box_game("fascia", 3.0, 0.14, 0.35, 0, 0, 0.60, M['rosso'])
    fl.box_game("zoccolo", 3.0, 0.14, 0.25, 0, 0, 0.0, M['crema'])
    fl.box_game("COL_recinto_basso", 3.0, 0.2, 1.05, 0, 0, 0, M['crema'])
    fl.export_glb("recinto_basso.glb")
    fl.render_previews("recinto_basso", ortho_scale=4)

if ASSET == 'raccordo_ovest':
    hv = 3.5 / 2   # semi-varco
    for s in (-1, 1):
        fl.box_game(f"ala_{s}", 8.0, 0.6, 4.0, s * (hv + 4.0), 0, 0, M['crema'])
        fl.box_game(f"COL_ala_{s}", 8.0, 0.6, 4.0, s * (hv + 4.0), 0, 0, M['crema'])
    fl.box_game("architrave", 3.5, 0.7, 1.0, 0, 0, 3.0, M['crema'])
    fl.box_game("COL_architrave", 3.5, 1.0, 1.0, 0, 0, 3.0, M['crema'])
    fl.export_glb("raccordo_ovest.glb")
    fl.render_previews("raccordo_ovest", ortho_scale=12)
```

- [ ] **Step 2: Render dei 4 asset + gate utente** (4 run con `-- recinzione`, `-- cancello`, `-- recinto_basso`, `-- raccordo_ovest`).
- [ ] **Step 3: funland-layout.py** — genera `funland-layout.json`: istanza `funland_base` a (63,−35,y−0.02) + recinzione lungo i segmenti (modulo 4 m, rotY dal segmento), cancelli e raccordo:

```python
import sys, os, json, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl

edifici = [{"modello": "funland_base", "x": 63.0, "z": -35.0, "rotY": 0.0, "y": -0.02, "s": 1.0}]

def fila(x0, z0, x1, z1, modulo=4.0, nome="recinzione"):
    # moduli lungo il segmento, centrati, rotY che allinea il lato-X del modello
    L = math.hypot(x1 - x0, z1 - z0)
    n = max(1, round(L / modulo))
    rot = math.degrees(math.atan2(-(z1 - z0), x1 - x0))   # convenzione three (x,z)
    for i in range(n):
        f = (i + 0.5) / n
        edifici.append({"modello": nome, "x": round(x0 + (x1 - x0) * f, 2),
                        "z": round(z0 + (z1 - z0) * f, 2), "rotY": round(rot, 1), "y": 0.0, "s": round(L / n / (4.0 if nome=="recinzione" else 3.0), 3)})

# SUD (l'arco al centro occupa x 51..60; a ovest di x≈48.5 chiudono i retri Jazz)
fila(48.5, -18.0, 51.0, -18.0)
fila(60.0, -18.0, 88.0, -18.0)
# EST con cancello a z=-36 (bocca 3.5 → moduli fino a -34.25 e da -37.75)
fila(88.0, -18.0, 88.0, -34.25)
edifici.append({"modello": "cancello", "x": 88.0, "z": -36.0, "rotY": 90.0, "y": 0.0, "s": 1.0})
fila(88.0, -37.75, 88.0, -52.0)
# NORD
fila(88.0, -52.0, 37.0, -52.0)
# DIAGONALE NO: dal capo nord al varco NE Jazz (il raccordo copre il giunto)
fila(37.0, -52.0, 31.8, -34.6)
# RACCORDO OVEST sul varco NE Jazz: varco locale lungo X → rotY = tangente del
# perimetro jazz in (32.5,-31.8) (radiale a 45.7° → tangente a ~135.7°)
rot_var = math.degrees(math.atan2(32.5, 31.8)) + 90.0
edifici.append({"modello": "raccordo_ovest", "x": 32.5, "z": -31.8, "rotY": round(rot_var, 1), "y": 0.0, "s": 1.0})

layout = {"edifici": edifici, "props": [],
          "vie": [{"nome": "funland", "asse": "x", "da": 38, "a": 88, "centro": -35, "larghezza": 34}]}
os.makedirs(fl.MODELS_DIR, exist_ok=True)
with open(fl.MODELS_DIR + "/funland-layout.json", "w", encoding="utf-8") as f:
    json.dump(layout, f, indent=2, ensure_ascii=False)
print("funland-layout.json:", len(edifici), "istanze")
```

(il campo `s` dei moduli fila adatta la lunghezza del modulo al segmento; se `loadZone` applica `s` uniforme su tutti gli assi e l'altezza ne risente troppo, tornare a moduli fissi 4 m con overlap agli angoli)

- [ ] **Step 4: Top render combinato REALE** — script `debug_ovest.py` sul modello di `debug_junction.py` esistente: scena con GLB jazz reali (istanze dell'anello NE da `zona-layout.json`) + recinzione/raccordo funland + sagome REF (disco r45.5, ovale piazza, croce galleria, corridoio servizio). Verificare: giunto ovest sigillato senza invadere il varco 3.5, fila sud che muore nei retri jazz senza buchi, cancello est allineato al corridoio srv (z=−36).
- [ ] **Step 5: Gate utente** sul top render.

---

### Task 5: Arco d'ingresso FUNLAND + apertura varco piazza

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/arco.py`
- Output: `frontend/assets/models/funland/arco_ingresso.glb`
- Modify: `frontend/assets/models/piazza/piazza-layout.json` (−3 istanze)
- Modify: `docs/superpowers/plans/blender-scripts/funland/funland-layout.py` (+1 istanza)

**Interfaces:**
- Produces: `arco_ingresso.glb` origine centro-base, bocca w5 h4 lungo X locale, fronte −z; COL = 2 spalle + architrave (anti-scavalco). Varco nord piazza APERTO (via cantiere + 2 muretti intrusi).

- [ ] **Step 1: Ricetta arco.py** — faccia da clown: spalle-torri, arco sopra la bocca, occhi/naso, insegna:

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()

for s in (-1, 1):   # spalle-torri ai lati della bocca (bocca w5 → x ±2.5..±4.3)
    fl.box_game(f"spalla_{s}", 1.8, 1.6, 5.2, s * 3.4, 0, 0, M['crema'])
    fl.box_game(f"COL_spalla_{s}", 1.8, 1.6, 5.2, s * 3.4, 0, 0, M['crema'])
    fl.box_game(f"fascia_{s}", 1.9, 1.7, 0.5, s * 3.4, 0, 3.2, M['rosso'])
    fl.lathe_profile(f"cupolino_{s}", [(0.85, 5.2), (0.80, 5.45), (0.5, 5.75), (0.0, 5.95)],
                     M['rosso'], segments=16)
    # lathe_profile centra sull'origine: spostare sul pilastro
    import bpy
    bpy.context.object.location.x = s * 3.4
# ARCO sopra la bocca (segmenti che seguono la curva) + architrave COL pieno
for i in range(7):
    a0, a1 = math.pi * i / 7, math.pi * (i + 1) / 7
    xa, ya = -2.5 * math.cos(a0), 4.0 + 1.1 * math.sin(a0)
    xb, yb = -2.5 * math.cos(a1), 4.0 + 1.1 * math.sin(a1)
    L = math.hypot(xb - xa, yb - ya) + 0.1
    fl.add_box_c(f"arco_{i}", L, 0.9, 0.55, (xa + xb) / 2, 0, (ya + yb) / 2,
                 M['rosso'] if i % 2 == 0 else M['crema'],
                 rot=(0, math.atan2(yb - ya, xb - xa), 0))
fl.box_game("COL_architrave", 5.0, 1.2, 2.0, 0, 0, 4.0, M['crema'])
# FACCIA: occhioni sopra l'arco + naso
import bpy
for s in (-1, 1):
    bpy.ops.mesh.primitive_uv_sphere_add(radius=0.55, location=(s * 1.1, -0.55, 5.6))
    e = bpy.context.object; e.name = f"occhio_{s}"; e.data.materials.append(M['bianco'])
    bpy.ops.mesh.primitive_uv_sphere_add(radius=0.22, location=(s * 1.1, -1.0, 5.6))
    p = bpy.context.object; p.name = f"pupilla_{s}"; p.data.materials.append(M['nero'])
bpy.ops.mesh.primitive_uv_sphere_add(radius=0.38, location=(0, -0.75, 4.95))
n = bpy.context.object; n.name = "naso"; n.data.materials.append(M['rosso'])
# DENTI appesi al bordo dell'arco (sorriso = la bocca stessa)
for i in range(5):
    fl.box_game(f"dente_{i}", 0.55, 0.25, 0.45, -1.7 + i * 0.85, -0.35, 3.75, M['bianco'])
fl.box_game("insegna_fondo", 4.6, 0.3, 1.0, 0, -0.7, 6.1, M['nero'])
fl.testo("txt_funland", "FUNLAND", 0.62, M['giallo'], (0, -0.88, 6.35))
fl.export_glb("arco_ingresso.glb")
fl.render_previews("arco_ingresso", ortho_scale=11, quarter_pos=(9, -10, 6))
```

- [ ] **Step 2: Render + iterazione + gate utente** (pezzo-firma del quartiere: iterare finché la faccia è leggibile e non inquietante nel senso sbagliato).
- [ ] **Step 3: Aprire il varco piazza** — da `frontend/assets/models/piazza/piazza-layout.json` rimuovere 3 istanze: `cantiere` (55.5,−16) e i 2 muretti DENTRO il varco w5: quello a (53.98,−16.17) e quello a (57.18,−16.13). Gli altri muretti nord (59.91,−14.8), (51.17,−14.86), (50.94,−14.68), (57.18… verificare di togliere solo i 2 indicati) restano.
- [ ] **Step 4: Istanza arco** — in funland-layout.py: `{"modello": "arco_ingresso", "x": 55.5, "z": -17.5, "rotY": 180.0, "y": 0.0, "s": 1.0}` (fronte verso la piazza). Rigenerare JSON.

---

### Task 6: Giostra centrale

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/giostra.py`
- Output: `frontend/assets/models/funland/giostra.glb`
- Modify: `funland-layout.py` (istanza a (56,−34))

**Interfaces:**
- Produces: `giostra.glb` origine centro-base: pedana Ø9, colonna centrale, tettoia a spicchi rosso/crema con guglia, 5 cavalli su pali; recinto quadrato 11×11 h1.05 (staccionata) TUTTO chiuso (non si sale, decisione spec). COL: 4 lati recinto (AABB) + colonna.

- [ ] **Step 1: Ricetta**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import bpy
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()

# PEDANA con bordo (lathe) + colonna a fusto sagomato + capitello
fl.lathe_profile("pedana", [(4.5, 0.0), (4.5, 0.42), (4.3, 0.5), (0.9, 0.5), (0.9, 0.0)],
                 M['legno'], segments=32)
fl.lathe_profile("colonna", [(0.8, 0.5), (0.75, 0.9), (0.5, 1.1), (0.5, 3.1),
                             (0.75, 3.3), (0.8, 3.6), (0.0, 3.6)], M['rosso'], segments=20)
# TETTOIA a 12 spicchi alternati + guglia con bandierina
for i in range(12):
    a = 2 * math.pi * (i + 0.5) / 12
    fl.add_box_c(f"spicchio_{i}", 2.6, 4.9 * math.tan(math.pi / 12) * 2 * 0.55, 0.12,
                 2.45 * math.sin(a), 2.45 * math.cos(a), 4.35,
                 M['rosso'] if i % 2 == 0 else M['crema'],
                 rot=(math.radians(-16) * math.cos(a), math.radians(16) * math.sin(a), -a))
fl.lathe_profile("cupola", [(0.55, 4.6), (0.4, 4.95), (0.15, 5.2), (0.0, 5.3)], M['ottone'], segments=16)
fl.add_cyl("asta", 0.04, 0.8, 0, 0, 5.3, M['ottone'], vertices=8)
fl.add_box_c("bandiera", 0.5, 0.03, 0.3, 0.28, 0, 5.9, M['rosso'])
# 5 CAVALLI su pali (r 3.2)
for i in range(5):
    a = 2 * math.pi * i / 5
    px, pz = 3.2 * math.sin(a), 3.2 * math.cos(a)   # blender bx, by
    fl.add_cyl(f"palo_{i}", 0.05, 3.0, px, pz, 0.5, M['ottone'], vertices=8)
    fl.skin_chain(f"cavallo_{i}", [(px - 0.45, pz, 1.55), (px, pz, 1.6), (px + 0.35, pz, 1.75), (px + 0.55, pz, 2.05)],
                  [(0.16, 0.2), (0.24, 0.28), (0.18, 0.22), (0.12, 0.14)],
                  M['bianco'] if i % 2 == 0 else M['salvia'])
    for g in range(4):
        fl.add_cyl(f"gamba_{i}_{g}", 0.05, 0.55, px - 0.35 + (g % 2) * 0.6, pz - 0.12 + (g // 2) * 0.24, 1.0, M['nero'], vertices=6)
# RECINTO quadrato 11×11 chiuso (staccionata bassa) + COL
for (nm, w, d, gx, gz) in (("n", 11, 0.2, 0, -5.5), ("s", 11, 0.2, 0, 5.5),
                            ("e", 0.2, 11, 5.5, 0), ("o", 0.2, 11, -5.5, 0)):
    fl.box_game(f"recinto_{nm}", w, d, 1.05, gx, gz, 0, M['crema'])
    fl.box_game(f"COL_recinto_{nm}", w, d, 1.05, gx, gz, 0, M['crema'])
fl.box_game("COL_colonna", 1.7, 1.7, 3.6, 0, 0, 0.5, M['rosso'])
fl.export_glb("giostra.glb")
fl.render_previews("giostra", ortho_scale=14, quarter_pos=(11, -12, 8))
```

- [ ] **Step 2: Render + iterazione + gate utente** (spicchi tettoia: se l'inclinazione a doppio rot non chiude bene, semplificare a cono lathe monocolore + festone bicolore al bordo).
- [ ] **Step 3: Istanza** — `{"modello": "giostra", "x": 56.0, "z": -34.0, "rotY": 15.0, "y": 0.0, "s": 1.0}`. Rigenerare JSON.

---

### Task 7: Autoscontro + macchinine

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/autoscontro.py`
- Create: `docs/superpowers/plans/blender-scripts/funland/macchinina.py`
- Output: `frontend/assets/models/funland/autoscontro.glb`, `macchinina.glb`
- Modify: `funland-layout.py` (pista + 6 macchinine)

**Interfaces:**
- Produces: `autoscontro.glb` origine centro-base: pista 14×10, pavimento metallico h0.12, muretto perimetrale h1.1 con 2 aperture w2.5 (centro lato OVEST e centro lato SUD), 4 pali angolari h5 con griglia-rete sopra, insegna "AUTO SCONTRO" sul lato nord. COL: 6 tratti muretto + 4 pali. `macchinina.glb`: bumper car 1.6×1.0 h0.55 + asta, `COL_macchinina` 1.6×1.0×0.75; colore via ARGV (`rosso|blu|salvia`) esportato come `macchinina_<col>.glb`… NO: un solo GLB `macchinina.glb` col corpo in materiale `blu_fiera` (la variante colore si rimanda: loadZone non parametrizza i materiali per istanza).

- [ ] **Step 1: Ricetta autoscontro.py**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
W, D, HM = 14.0, 10.0, 1.1     # pista e muretto
fl.box_game("pav_pista", W, D, 0.12, 0, 0, 0, M['metallo'])
APER = 2.5
# muretto: lato NORD pieno, EST pieno, OVEST e SUD con apertura centrale
fl.box_game("muro_n", W + 0.5, 0.5, HM, 0, -D / 2, 0, M['rosso'])
fl.box_game("COL_muro_n", W + 0.5, 0.5, HM, 0, -D / 2, 0, M['rosso'])
fl.box_game("muro_e", 0.5, D, HM, W / 2, 0, 0, M['rosso'])
fl.box_game("COL_muro_e", 0.5, D, HM, W / 2, 0, 0, M['rosso'])
for s in (-1, 1):   # SUD in 2 tratti (apertura al centro)
    L = (W + 0.5 - APER) / 2
    fl.box_game(f"muro_s_{s}", L, 0.5, HM, s * (APER / 2 + L / 2), D / 2, 0, M['rosso'])
    fl.box_game(f"COL_muro_s_{s}", L, 0.5, HM, s * (APER / 2 + L / 2), D / 2, 0, M['rosso'])
    Lo = (D - APER) / 2   # OVEST in 2 tratti
    fl.box_game(f"muro_o_{s}", 0.5, Lo, HM, -W / 2, s * (APER / 2 + Lo / 2), 0, M['rosso'])
    fl.box_game(f"COL_muro_o_{s}", 0.5, Lo, HM, -W / 2, s * (APER / 2 + Lo / 2), 0, M['rosso'])
fl.box_game("bordo_top", W + 0.7, 0.2, 0.12, 0, -D / 2, HM, M['ottone'])   # corrimano nord (estetica)
for (sx, sz) in ((-1, -1), (1, -1), (-1, 1), (1, 1)):    # pali + rete
    fl.box_game(f"palo_{sx}_{sz}", 0.3, 0.3, 5.0, sx * (W / 2 - 0.3), sz * (D / 2 - 0.3), 0, M['nero'])
    fl.box_game(f"COL_palo_{sx}_{sz}", 0.3, 0.3, 5.0, sx * (W / 2 - 0.3), sz * (D / 2 - 0.3), 0, M['nero'])
for i in range(6):
    fl.box_game(f"rete_x_{i}", W - 0.6, 0.05, 0.05, 0, -D / 2 + 0.3 + i * (D - 0.6) / 5, 4.9, M['nero'])
for i in range(8):
    fl.box_game(f"rete_z_{i}", 0.05, D - 0.6, 0.05, -W / 2 + 0.3 + i * (W - 0.6) / 7, 0, 4.9, M['nero'])
fl.box_game("insegna_fondo", 6.0, 0.25, 1.1, 0, -D / 2 - 0.1, 5.0, M['nero'])
fl.testo("txt_pista", "AUTO SCONTRO", 0.55, M['giallo'], (0, D / 2 + 0.28, 5.3))
fl.export_glb("autoscontro.glb")
fl.render_previews("autoscontro", ortho_scale=18, quarter_pos=(14, -15, 9))
```

(NB coordinate testo: il lato nord del modello è a gz=−D/2 → by=+D/2; verificare nel render che l'insegna guardi FUORI dal lato nord)

- [ ] **Step 2: Ricetta macchinina.py**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import bpy
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
# scocca a goccia (lathe schiacciato con sx), paraurti, sedile, volante, asta
fl.lathe_profile("scocca", [(0.78, 0.10), (0.80, 0.28), (0.72, 0.42), (0.45, 0.52), (0.0, 0.55)],
                 M['blu'], segments=20)
sc = bpy.context.object
sc.scale = (1.0, 0.62, 1.0)          # in blender: y = profondità (z gioco)
fl.lathe_profile("paraurti", [(0.86, 0.06), (0.88, 0.16), (0.86, 0.26), (0.82, 0.28)],
                 M['nero'], segments=20)
pu = bpy.context.object
pu.scale = (1.0, 0.62, 1.0)
fl.box_game("sedile", 0.5, 0.35, 0.35, 0, 0.12, 0.30, M['rosso'])
fl.add_cyl("piantone", 0.035, 0.35, 0, -0.22, 0.42, M['nero'], vertices=8)
fl.add_cyl("volante", 0.14, 0.05, 0, -0.22, 0.75, M['nero'], vertices=12)
fl.add_cyl("asta", 0.035, 1.5, 0, 0.55, 0.4, M['ottone'], vertices=8)
fl.box_game("pattino", 0.22, 0.1, 0.06, 0, 0.55, 1.9, M['nero'])
fl.box_game("COL_macchinina", 1.7, 1.1, 0.75, 0, 0, 0, M['blu'])
fl.export_glb("macchinina.glb")
fl.render_previews("macchinina", ortho_scale=3)
```

- [ ] **Step 3: Render + gate utente** su entrambi.
- [ ] **Step 4: Istanze** — in funland-layout.py:

```python
edifici.append({"modello": "autoscontro", "x": 72.0, "z": -32.0, "rotY": 0.0, "y": 0.0, "s": 1.0})
for (mx, mz, mr) in ((67.0, -30.0, 20), (74.5, -29.0, 160), (70.0, -34.5, 75),
                     (76.5, -35.0, 290), (71.5, -31.5, 120), (63.0, -24.0, 205)):
    edifici.append({"modello": "macchinina", "x": mx, "z": mz, "rotY": mr, "y": 0.0, "s": 1.0})
```

(l'ultima è "in panne" fuori pista, sul viale sud-est). Rigenerare JSON.

---

### Task 8: Ruota panoramica

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/ruota.py`
- Output: `frontend/assets/models/funland/ruota.glb`
- Modify: `funland-layout.py` (istanza a (78,−46))

**Interfaces:**
- Produces: `ruota.glb` origine centro-base: 2 piloni ad A, mozzo a y11, cerchione Ø18 nel piano X-Y locale (16 segmenti), 8 raggi, 8 cabine appese di cui 2 BASSE vicino a terra con COL (coperture); COL: 4 zampe piloni + 2 cabine basse. Tutto il resto solo visivo.

- [ ] **Step 1: Ricetta**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
HUB, R = 11.0, 9.0
for s in (-1, 1):        # piloni ad A nel piano trasversale (profondità z gioco)
    for d in (-1, 1):
        fl.add_box_c(f"zampa_{s}_{d}", 0.55, 0.55, 11.8, s * 1.6, d * 2.6, 5.6, M['rosso'],
                     rot=(d * math.radians(13), 0, 0))
        fl.box_game(f"COL_zampa_{s}_{d}", 0.8, 1.2, 4.0, s * 1.6, d * 2.0, 0, M['rosso'])
    fl.add_box_c(f"traversa_{s}", 0.4, 4.4, 0.4, s * 1.6, 0, 6.0, M['rosso'], rot=(math.radians(90), 0, 0))
fl.add_cyl("mozzo", 0.5, 3.8, 0, 0, HUB, M['ottone'], vertices=16)   # asse lungo?? → vedi nota
import bpy
bpy.context.object.rotation_euler = (math.radians(90), 0, 0)          # asse orizzontale (profondità)
bpy.context.object.location = (0, 0, HUB)
# CERCHIONE: 16 segmenti nel piano verticale X-Z blender (E-O di gioco)
for i in range(16):
    a0, a1 = 2 * math.pi * i / 16, 2 * math.pi * (i + 1) / 16
    xa, ya = R * math.cos(a0), HUB + R * math.sin(a0)
    xb, yb = R * math.cos(a1), HUB + R * math.sin(a1)
    L = math.hypot(xb - xa, yb - ya) + 0.1
    fl.add_box_c(f"cerchio_{i}", L, 0.28, 0.28, (xa + xb) / 2, 0, (ya + yb) / 2, M['crema'],
                 rot=(0, math.atan2(yb - ya, xb - xa), 0))
for i in range(8):       # raggi
    a = 2 * math.pi * i / 8
    fl.add_box_c(f"raggio_{i}", R, 0.16, 0.16, R / 2 * math.cos(a), 0, HUB + R / 2 * math.sin(a),
                 M['ottone'], rot=(0, a, 0))
def cabina(tag, gx, gy, col, with_col):
    fl.box_game(f"cab_{tag}", 1.5, 1.4, 1.5, gx, 0, gy, col)
    fl.box_game(f"cab_tetto_{tag}", 1.7, 1.6, 0.18, gx, 0, gy + 1.5, M['rosso'], bevel=0.06)
    fl.add_cyl(f"cab_gancio_{tag}", 0.05, 0.6, gx, 0, gy + 1.68, M['nero'], vertices=6)
    if with_col:
        fl.box_game(f"COL_cab_{tag}", 1.5, 1.4, 1.7, gx, 0, gy, col)
for i in range(8):       # 8 cabine appese al cerchione; le 2 in basso toccano terra
    a = 2 * math.pi * (i + 0.5) / 8
    cx, cy = R * math.cos(a), HUB + R * math.sin(a) - 2.1
    at_ground = cy < 1.2
    cabina(str(i), cx, max(0.0, cy), M['salvia'] if i % 2 == 0 else M['blu'], at_ground)
fl.export_glb("ruota.glb")
fl.render_previews("ruota", ortho_scale=26, quarter_pos=(20, -22, 14))
```

(nota mozzo: `add_cyl` crea il cilindro verticale — la rotazione X 90° lo mette orizzontale lungo la profondità; verificare nel render che attraversi i due piloni)

- [ ] **Step 2: Render + iterazione + gate utente** (proporzioni: la ruota deve svettare sopra la recinzione ed essere leggibile dalla piazza).
- [ ] **Step 3: Istanza** — `{"modello": "ruota", "x": 78.0, "z": -46.0, "rotY": 0.0, "y": 0.0, "s": 1.0}` (piano ruota E-O, visibile frontalmente arrivando da sud). Rigenerare JSON.

---

### Task 9: Area ristoro + tiro a segno

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/ristoro.py` (churros + tavolino via ARGV)
- Create: `docs/superpowers/plans/blender-scripts/funland/tiro.py`
- Output: `frontend/assets/models/funland/chiosco_churros.glb`, `tavolino.glb`, `tiro_a_segno.glb`
- Modify: `funland-layout.py` (istanze)

**Interfaces:**
- Produces: `chiosco_churros.glb` (Ø2.6 h2.4 + tenda conica a strisce, bancone, insegna "CHURROS"; `COL_churros` 2.8×2.8×3.4), `tavolino.glb` (tavolo tondo + ombrellone a strisce; `COL_tavolino` 1.2×1.2×0.8 — copertura bassa), `tiro_a_segno.glb` (tendone 8×4 h4.5 fronte −z: bancone h1.0 davanti = copertura crouch, fianchi e fondale pieni = non si attraversa, 3 sagome papera dietro il bancone, insegna "TIRO A SEGNO").

- [ ] **Step 1: Ricetta ristoro.py**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import bpy
import funland_lib as fl
ASSET = 'churros'
if '--' in sys.argv:
    ASSET = sys.argv[sys.argv.index('--') + 1]
fl.clear_scene()
M = fl.materiali()

if ASSET == 'churros':
    fl.add_cyl("corpo", 1.3, 2.4, 0, 0, 0, M['salvia'], vertices=16)
    for i in range(6):
        a = 2 * math.pi * i / 6
        fl.add_box_c(f"lesena_{i}", 0.14, 0.08, 2.4, 1.32 * math.sin(a), 1.32 * math.cos(a), 1.2,
                     M['crema'], rot=(0, 0, -a))
    fl.box_game("bancone", 1.6, 0.4, 0.12, 0, -1.45, 1.05, M['legno'])
    fl.box_game("sportello", 1.3, 0.1, 0.9, 0, -1.31, 1.15, M['crema'])
    for i in range(12):   # tenda conica a spicchi alternati
        a = 2 * math.pi * (i + 0.5) / 12
        fl.add_box_c(f"tenda_{i}", 1.5, 0.85, 0.08, 1.05 * math.sin(a), 1.05 * math.cos(a), 2.75,
                     M['rosso'] if i % 2 == 0 else M['crema'],
                     rot=(math.radians(-22) * math.cos(a), math.radians(22) * math.sin(a), -a))
    fl.lathe_profile("puntale", [(0.12, 3.1), (0.05, 3.3), (0.0, 3.4)], M['ottone'], segments=12)
    fl.testo("txt_churros", "CHURROS", 0.26, M['giallo'], (0, 1.42, 1.9))
    fl.box_game("COL_churros", 2.8, 2.8, 3.4, 0, 0, 0, M['salvia'])
    fl.export_glb("chiosco_churros.glb")
    fl.render_previews("chiosco_churros", ortho_scale=6)

if ASSET == 'tavolino':
    fl.add_cyl("piano", 0.6, 0.06, 0, 0, 0.72, M['crema'], vertices=16)
    fl.add_cyl("gamba", 0.06, 0.72, 0, 0, 0, M['nero'], vertices=8)
    fl.lathe_profile("base", [(0.3, 0.0), (0.28, 0.06), (0.08, 0.1), (0.06, 0.14)], M['nero'], segments=12)
    fl.add_cyl("palo_omb", 0.04, 1.9, 0, 0, 0.78, M['ottone'], vertices=8)
    for i in range(8):
        a = 2 * math.pi * (i + 0.5) / 8
        fl.add_box_c(f"omb_{i}", 1.3, 0.95, 0.05, 0.62 * math.sin(a), 0.62 * math.cos(a), 2.5,
                     M['rosso'] if i % 2 == 0 else M['bianco'],
                     rot=(math.radians(-18) * math.cos(a), math.radians(18) * math.sin(a), -a))
    for s in (-1, 1):
        fl.add_cyl(f"sgabello_{s}", 0.22, 0.45, s * 0.85, 0, 0, M['legno'], vertices=10)
    fl.box_game("COL_tavolino", 1.2, 1.2, 0.8, 0, 0, 0, M['crema'])
    fl.export_glb("tavolino.glb")
    fl.render_previews("tavolino", ortho_scale=4)
```

- [ ] **Step 2: Ricetta tiro.py**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
W, D, H = 8.0, 4.0, 4.5      # fronte −z (bancone), fondale +z
fl.box_game("pedana", W, D, 0.14, 0, 0, 0, M['legno'])
fl.box_game("bancone", W - 0.8, 0.5, 1.0, 0, -D / 2 + 0.35, 0.14, M['rosso'])
fl.box_game("COL_bancone", W - 0.8, 0.5, 1.0, 0, -D / 2 + 0.35, 0.14, M['rosso'])
fl.box_game("fondale", W, 0.3, 3.4, 0, D / 2 - 0.2, 0.14, M['verde'])
fl.box_game("COL_fondale", W, 0.3, 3.4, 0, D / 2 - 0.2, 0.14, M['verde'])
for s in (-1, 1):
    fl.box_game(f"fianco_{s}", 0.3, D, 3.4, s * (W / 2 - 0.15), 0, 0.14, M['verde'])
    fl.box_game(f"COL_fianco_{s}", 0.3, D, 3.4, s * (W / 2 - 0.15), 0, 0.14, M['verde'])
for i in range(3):    # sagome papera sul ripiano del fondale
    px = -2.2 + i * 2.2
    fl.skin_chain(f"papera_{i}", [(px - 0.25, 1.55, 1.7), (px, 1.55, 1.75), (px + 0.2, 1.55, 2.0), (px + 0.38, 1.55, 2.05)],
                  [(0.14, 0.18), (0.2, 0.24), (0.09, 0.1), (0.11, 0.12)], M['giallo'])
for i in range(9):    # tetto a falda a strisce (pende verso il fronte)
    fl.add_box_c(f"falda_{i}", 0.9, D + 1.2, 0.08, -3.6 + i * 0.9, 0, 4.3,
                 M['rosso'] if i % 2 == 0 else M['crema'], rot=(math.radians(-12), 0, 0))
fl.box_game("insegna_fondo", 5.2, 0.2, 0.9, 0, -D / 2 - 0.15, 4.6, M['nero'])
fl.testo("txt_tiro", "TIRO A SEGNO", 0.5, M['giallo'], (0, D / 2 + 0.32, 4.82))
fl.export_glb("tiro_a_segno.glb")
fl.render_previews("tiro_a_segno", ortho_scale=11, quarter_pos=(8, -9, 6))
```

- [ ] **Step 3: Render dei 3 asset + gate utente.**
- [ ] **Step 4: Istanze** — in funland-layout.py:

```python
edifici.append({"modello": "chiosco_churros", "x": 48.5, "z": -24.0, "rotY": 250.0, "y": 0.0, "s": 1.0})
for (tx, tz, tr) in ((46.5, -27.0, 15), (50.5, -27.5, 80), (47.0, -21.0, 200)):
    edifici.append({"modello": "tavolino", "x": tx, "z": tz, "rotY": tr, "y": 0.0, "s": 1.0})
edifici.append({"modello": "tiro_a_segno", "x": 44.0, "z": -40.0, "rotY": 270.0, "y": 0.0, "s": 1.0})
```

(churros con sportello verso il viale; tiro a segno col bancone verso EST/giostra — verificare i due rotY nel top render, la convenzione fronte=−z ruotata di 250/270 va controllata a vista). ⚠️ Le posizioni ristoro sono a ridosso dei retri Jazz: validare sul render combinato del Task 10 e spostare a est se compenetrano.

---

### Task 10: Coaster-fondale + recinti bassi + top render finale

**Files:**
- Create: `docs/superpowers/plans/blender-scripts/funland/coaster.py`
- Output: `frontend/assets/models/funland/coaster_fondale.glb`
- Modify: `funland-layout.py` (istanze coaster + recinti bassi + render finale)

**Interfaces:**
- Produces: `coaster_fondale.glb` = modulo scenografico 24 m (binario a colline: 2 tubi skin_chain paralleli + traversine + tralicci), SOLO mesh visive, NESSUNA COL. Layout completo con 3 moduli a z≈−55 + 3 `recinto_basso` spezza-sightline + top render debug completo.

- [ ] **Step 1: Ricetta coaster.py**

```python
import sys, math
sys.path.insert(0, "C:/Users/pacia/Desktop/Claude Workspace/prova/docs/superpowers/plans/blender-scripts/funland")
import funland_lib as fl
fl.clear_scene()
M = fl.materiali()
def hill(x):     # profilo colline: 2 gobbe asimmetriche su 24 m
    return 3.2 + 4.6 * math.exp(-((x + 6.5) / 3.4) ** 2) + 6.8 * math.exp(-((x - 4.5) / 4.2) ** 2)
PTS = [(-12.0 + i * 0.75, hill(-12.0 + i * 0.75)) for i in range(33)]
for off in (-0.55, 0.55):    # 2 rotaie
    fl.skin_chain(f"rotaia_{int(off*100)}", [(x, off, y) for (x, y) in PTS],
                  [(0.09, 0.09)] * len(PTS), M['rosso'])
for i in range(0, 32, 2):    # traversine
    (x, y) = PTS[i]
    fl.add_box_c(f"trav_{i}", 0.14, 1.4, 0.1, x, 0, y - 0.12, M['legno'])
for i in range(0, 33, 4):    # tralicci: montanti + croce
    (x, y) = PTS[i]
    for s in (-1, 1):
        fl.add_box_c(f"mont_{i}_{s}", 0.22, 0.22, y - 0.3, x + s * 0.45, s * 0.45, 0, M['bianco'])
    fl.add_box_c(f"croce_{i}", 0.12, 1.5, 0.12, x, 0, y * 0.5, M['bianco'], rot=(math.radians(40), 0, 0))
fl.export_glb("coaster_fondale.glb")
fl.render_previews("coaster_fondale", ortho_scale=28, quarter_pos=(20, -20, 12))
```

(nota: `add_box_c` con base a bz — verificare la firma: se prende il CENTRO, i montanti vanno a y/2; adeguare come già fatto negli altri script)

- [ ] **Step 2: Render + gate utente** (deve leggersi come skyline, con la gobba alta ~10 m).
- [ ] **Step 3: Istanze finali** — in funland-layout.py:

```python
for (cx, rot) in ((48.0, 2.0), (66.0, -3.0), (82.0, 4.0)):
    edifici.append({"modello": "coaster_fondale", "x": cx, "z": -55.0, "rotY": rot, "y": 0.0, "s": 1.0})
for (bx, bz, br) in ((64.0, -39.5, 45), (61.0, -44.0, 10), (80.5, -23.5, 90)):
    edifici.append({"modello": "recinto_basso", "x": bx, "z": bz, "rotY": br, "y": 0.0, "s": 1.0})
```

(recinti bassi: spezzano la diagonale ingresso↔ruota e la corsia est verso il cancello)
- [ ] **Step 4: Top render debug COMPLETO** — in coda a funland-layout.py (flag `RENDER=True`): scena con tutte le istanze come box grezzi + sagome REF (disco Jazz r45.5, ovale piazza, croce Galleria, corridoio srv a z=−36) + camera top ortho scale ~120 su (60,−35). Verificare a vista le sightline: la retta arco(55.5,−18)→ruota(78,−46) e la corsia est devono intersecare recinti/pista.
- [ ] **Step 5: Render combinato REALE dei giunti** (riuso `debug_ovest.py` del Task 4 esteso con tutte le istanze funland + jazz NE reali) + **gate utente sul layout completo**.

---

### Task 11: Integrazione fps.js + verifica in-engine

**Files:**
- Modify: `frontend/fps.js:30-35` (ceilingAt), `frontend/fps.js:3155-3156` (rete di sicurezza), `frontend/fps.js:3422-3429` (_boot)

**Interfaces:**
- Consumes: `funland-layout.json` + GLB in `frontend/assets/models/funland/`.
- Produces: Funland caricato nel mondo EXTENDED, confini e soffitto corretti.

- [ ] **Step 1: Soffitto per-zona** — in `ceilingAt` (riga 31), PRIMA del check Galleria:

```js
function ceilingAt(x, z) {
    if (x > 30 && x < 92 && z < -16 && z > -58) return MAP_CEIL;   // Funland (cielo aperto)
    if (Math.hypot(x - GALLERIA_OFF.x, z - GALLERIA_OFF.z) < 34) return GALLERIA_CEIL;
    if (Math.hypot(x, z) < 50) return MAP_CEIL;
    return CORR_CEIL;
}
```

(il corridoio servizio x>92 ricade in CORR_CEIL come i flank: coerente)

- [ ] **Step 2: Rete di sicurezza** — riga 3156: il mondo ora arriva a z=−58 (coaster incluso):

```js
        pos.x = Math.max(-50, Math.min(131, pos.x));
        pos.z = Math.max(-58, Math.min(50, pos.z));
```

- [ ] **Step 3: loadZone** — nella `Promise.all` del ramo EXTENDED (riga 3428, dopo la piazza):

```js
            loadZone('assets/models/funland/', 'funland-layout.json', { pav: false }),
```

- [ ] **Step 4: Sintassi** — Run: `node --check frontend/fps.js` → exit 0.
- [ ] **Step 5: Screenshot harness** (Chrome swiftshader + puppeteer dello scratchpad, server dell'utente acceso — chiederglielo). Viste: (55.5,−20) verso nord (viale+giostra+ruota+coaster skyline), (56,−30) verso nord, (84,−34) verso ovest (pista+parco), (92,−36) verso est (corridoio servizio), (55.5,−12) verso nord DALLA piazza (arco FUNLAND al posto del cantiere). Expected: nessun buco sul void, console senza errori (`🏙 Zona assets/models/funland/`).
- [ ] **Step 6: Collisioni smoke** (evaluate nella stessa sessione harness): (88.5,−25) → respinto a x<87.8 (recinzione est); (72,−37.5) → respinto o step SOLO attraverso le aperture della pista; (56,−34) accanto alla giostra → non entra nel recinto 11×11; attraversamento porte: piazza→arco→parco, parco→cancello est→corridoio→galleria, parco→raccordo ovest→Jazz (posa e cammino scriptato, posizione finale oltre la soglia).
- [ ] **Step 7: Regressione** — `?map=jazz` carica ancora la sola Jazz (nessun errore); flank SUD percorribile ((60, 38) → si cammina fino a (95, 38)).

---

### Task 12: Spawn server + fps-notes + gate finale

**Files:**
- Modify: `backend/sockets/games/fpsGameSocket.js:39-42` (SPAWN_POINTS, dopo il blocco PIAZZA)
- Modify: `docs/fps-notes.md` (sezione Mappa + conteggio spawn)

**Interfaces:**
- Consumes: convenzione `angle = atan2(x−cx, z−cz)` per guardare (cx,cz).
- Produces: 23 spawn totali; docs aggiornate.

- [ ] **Step 1: Aggiungere gli spawn** (tutti rivolti alla giostra (56,−34)):

```js
    // — FUNLAND (luna park nord, recinto x 38..88 / z -52..-18) —
    { x: 55.5, y: 0, z: -21, angle: Math.atan2(55.5 - 56, -21 + 34) },  // gola d'ingresso
    { x: 44,   y: 0, z: -40, angle: Math.atan2(44 - 56,   -40 + 34) },  // tiro a segno
    { x: 78,   y: 0, z: -44, angle: Math.atan2(78 - 56,   -44 + 34) },  // base ruota
    { x: 84,   y: 0, z: -27, angle: Math.atan2(84 - 56,   -27 + 34) }   // bordo pista autoscontro
];
```

(ricordare la virgola dopo l'ultimo spawn piazza esistente)

- [ ] **Step 2: Sintassi** — `node --check backend/sockets/games/fpsGameSocket.js` → exit 0. Riavvio server = utente.
- [ ] **Step 3: Aggiornare `docs/fps-notes.md`** — nella sezione Mappa: nuovo paragrafo "### Luna Park Funland (quartiere nord, 2026-07-10)" (recinto 50×38, 3 porte, flank nord DEMOLITO e sostituito da anello esterno + corridoio servizio est, attrazioni statiche, coaster fondale senza COL); aggiornare il paragrafo Piazza (cantiere rimosso → arco Funland); correggere il conteggio spawn 19→23; nota `ceilingAt`/rete di sicurezza estese.
- [ ] **Step 4: GATE FINALE UTENTE** — partita 2 client in localhost: piazza→arco→parco, giro dell'anello esterno Jazz↔Galleria, pista autoscontro (aperture+coperture), corridoio servizio con porta verde, flank sud intatto, spawn nuovi, nessun buco visivo. Solo dopo l'ok dell'utente il sotto-progetto B è chiuso (commit = utente).

---

## Self-review (fatta)

- **Coverage spec**: demolizione fN + servizio est (T2), footprint/recinto/3 porte (T4-T5), pavimentazione+gola (T3), arco (T5), giostra (T6), autoscontro+macchinine (T7), ruota (T8), ristoro+tiro (T9), coaster fondale+sightline (T10), loadZone/safety net/ceiling (T11), spawn+notes+regressione+gate (T11-T12). Fuori scope rispettato (niente casa orrori, niente animazioni, niente climb).
- **Placeholder**: tutte le ricette sono punti di partenza completi ed eseguibili; l'iterazione estetica è un ciclo esplicito con gate utente (pipeline visiva, come piazza/armi).
- **Coerenza**: nomi modello (`funland_base, recinzione, cancello, recinto_basso, raccordo_ovest, arco_ingresso, giostra, autoscontro, macchinina, ruota, chiosco_churros, tavolino, tiro_a_segno, coaster_fondale`) identici tra ricette e layout; costanti geometria centralizzate in funland_lib (T1) e usate da T3-T10; cancello est (88,−36) allineato al corridoio srv del T2; rect `inFunland` di T11 (30..92, −58..−16) racchiude recinto+corridoi+coaster.
- **Rischio segnalato**: profondità reale dei retri Jazz sul lato SO (ristoro, fila sud) — mitigato con render combinato reale (T4/T10) e possibilità di spostare il ristoro a est.
